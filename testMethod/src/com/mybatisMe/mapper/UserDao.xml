<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatisMe.mapper">
	<cache />

	<select id="selectUser" resultType="user" parameterType="int">
		select * from user WHERE ID = #{id}
	</select>
	
	<insert id="insertUser" useGeneratedKeys="true" keyProperty="id" parameterType="user">
		INSERT INTO USER (NAME,AGE) VALUES (#{name},#{age})
	</insert>

	<insert id="insertUsers" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO USER (NAME,AGE) VALUES 
		<foreach item="userEntity" collection="list" separator=",">
		(#{userEntity。name},#{userEntity。age})
		</foreach>
	</insert>
</mapper>

